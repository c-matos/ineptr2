# Normalize dimension filters for consistent comparison

Normalize dimension filters for consistent comparison

## Usage

``` r
normalize_dim_filters(filters)
```

## Arguments

- filters:

  Named list from `...` (e.g.,
  `list(dim1 = "S7A2022", dim2 = c("11","17"))`)

## Value

Named list with lowercase names and sorted character values
